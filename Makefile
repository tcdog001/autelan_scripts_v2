all:build

build:
	@echo "warning: only support make install!"
clean:
	@echo "warning: only support make install!"
install:
	./autelan.rootfs install
uninstall:
	@echo "warning: only support make install!"
